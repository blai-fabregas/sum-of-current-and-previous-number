#assigning the current number
for n in range (1,11):
    current_number = n
    print("The current number is: ",current_number)

#assigning how to find the previous number
    previous_number = n - 1

#adding if function to handle the special case for the first iteration where there is no previous number.
    if n > 1 else 0

#printing the sum of the current and previous number
sum = current_number + previous_number
print ("The sum of the cuurent number and previous number is", sum)